export const SPLITTER_PANEL_STYLES = `
  :host {
    box-sizing: border-box;
    display: block;
    min-height: 0;
    min-width: 0;
    overflow: auto;
  }

  *, *::before, *::after {
    box-sizing: border-box;
  }

  .ds-splitter-panel {
    height: 100%;
    min-height: 0;
    min-width: 0;
    width: 100%;
  }
`;

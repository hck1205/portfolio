import type { ViewerControlConfig } from "./GraphicIntegrationWorkspace.types";

export const DEFAULT_VIEWER_CONTROL_CONFIG: ViewerControlConfig = {
  autoRotate: false,
  canZoom: true,
  materialTint: "#ffffff",
  showEnvironment: true,
  showGrid: true,
  useDamping: true
};
